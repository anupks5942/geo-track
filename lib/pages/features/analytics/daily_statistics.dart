import 'package:flutter/material.dart';

class DailyStatisticsScreen extends StatelessWidget {
  const DailyStatisticsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Daily Statistics"),
      ),
    );
  }
}
