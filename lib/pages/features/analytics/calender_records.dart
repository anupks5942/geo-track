import 'package:flutter/material.dart';

class CalenderRecordsScreen extends StatelessWidget {
  const CalenderRecordsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Calender Records"),
      ),
    );
  }
}
