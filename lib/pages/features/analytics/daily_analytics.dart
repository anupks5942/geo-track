import 'package:flutter/material.dart';

class DailyAnalyticsScreen extends StatelessWidget {
  const DailyAnalyticsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Daily Analytics"),
      ),
    );
  }
}
