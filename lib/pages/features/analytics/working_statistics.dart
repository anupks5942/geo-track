import 'package:flutter/material.dart';

class WorkingStatisticsScreen extends StatelessWidget {
  const WorkingStatisticsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Working Statistics"),
      ),
    );
  }
}
